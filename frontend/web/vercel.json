{
  "buildCommand": "npm install -g pnpm@10 && cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @zahavi/web build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
